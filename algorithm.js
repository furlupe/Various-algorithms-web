import {circles} from './canvas.js';
function geneticAlgorithm(){
    let generations = [];
    console.log(circles);

}

// function firstGeneration (circles) {
//     return shuffle(circles);
// }

// function distance(x1, y1, x2, y2) {
//     let xDist;
//     let yDist;
//     let Dist;
//     xDist = Math.abs(x1 - x2);
//     yDist = Math.abs(y1 - y2);
//     Dist = Math.sqrt((Math.pow(xDist, 2)) + (Math.pow(yDist, 2)));
//     return Dist;
// }